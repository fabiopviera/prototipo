---
title: "Protótipo DRG - Ortopedia"
output: 
  flexdashboard::flex_dashboard:
     social: menu
     source_code: embed    
     vertical_layout: fill
     runtime: shiny
---

```{r setup, include=FALSE}
library(flexdashboard)
library(shiny)
library(DT)
library(writexl)
library(readxl)
```

Teste 1
=====================================  


Inputs {.sidebar data-width=250}
-----------------------------------------------------------------------

```{r}

fileInput("file1", "Select xlsx File to Import", accept = ".xlsx")

actionButton("run_button", "Run Analysis", icon = icon("play"), style = "width:150px")

br()
br()

actionButton("delete_button", "Clear Analysis", icon = icon("trash"), style = "width:150px")
```

Column {.tabset}
-----------------------------------------------------------------------


### Entrada

```{r}
renderTable({
      file <- input$file1
      ext <- tools::file_ext(file$datapath)
      
      req(file)
      validate(need(ext == "xlsx", "Please upload a csv file"))
      
      data_ <- read_xlsx(file$datapath) 
      #%>%  datatable(options = list(pageLength = 25), rownames = FALSE)
      data_ 
    })
```



### Saída

```{r}
#eventReactive
renderDT({
  datatable(read_xlsx(file$datapath), extensions = "Bottons", options = list(dom = 'Blfrtip',
                                                          bottons = c('copy', 'csv', 'print'),
                                                          lengthMenu = list(c(10,25,50,-1),c(10, 25, 50, "All"))))
})
```

